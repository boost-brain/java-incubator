В чистой VPS машине:
sudo apt-get -y install fail2ban
sudo apt-get -y install mc
sudo apt-get -y install default-jdk
apt-get -y install maven
sudo apt-get -y install \
     apt-transport-https \
     ca-certificates \
     curl \
     gnupg-agent \
     software-properties-common
curl -fsSL https://download.docker.com/linux/ubuntu/gpg | sudo apt-key add -
sudo apt-key fingerprint 0EBFCD88
sudo add-apt-repository \
    "deb [arch=amd64] https://download.docker.com/linux/ubuntu \
    $(lsb_release -cs) \
    stable"
sudo apt-get update
sudo apt-get -y install docker-ce docker-ce-cli containerd.io
sudo curl -L
  "https://github.com/docker/compose/releases/download/1.26.0/docker-compose-$(uname
  -s)-$(uname -m)" -o /usr/local/bin/docker-compose
sudo chmod +x /usr/local/bin/docker-compose
apt-get install git
curl -sL https://deb.nodesource.com/setup_10.x | sudo -E bash -
sudo apt-get install nodejs
wget -qO-
https://raw.githubusercontent.com/creationix/nvm/v0.33.8/install.sh | bash
nvm install 12.18.0
curl -sS https://dl.yarnpkg.com/debian/pubkey.gpg | sudo apt-key add -
echo "deb https://dl.yarnpkg.com/debian/ stable main" | sudo tee
/etc/apt/sources.list.d/yarn.list
sudo apt-get update
sudo apt-get -y install yarn

В Home или где больше нравится
git clone  https://github.com/boost-brain/java-incubator.git -b
indiancode

В папке \java-incubator\students-registry\frontend\src\api в файле api.js меняем адрес сервера на текущий.

Переходим в корень папки students-registry , там запускаем
mvn install -DskipTests=true -PDocker
docker-compose up -d --build
ЖДкем пока всё запустится. Пользуемся
